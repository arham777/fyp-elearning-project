# Django settings
SECRET_KEY=dev-insecure-secret-key-change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Cloudinary (for video/media uploads)
# Get your credentials from https://cloudinary.com/console
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME

# Database selection
# Set USE_SQLITE=True for quick local dev. Set to False to use PostgreSQL vars below.
USE_SQLITE=True

# PostgreSQL settings (used when USE_SQLITE=False)
DB_ENGINE=postgresql
DB_NAME=elearningdb
DB_USER=adminuser
DB_PASSWORD=Uol01001010
DB_HOST=localhost
DB_PORT=5432

# CORS settings
CORS_ALLOW_ALL_ORIGINS=True
# If you prefer to restrict origins, set the above to False and list them below
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
